# Agentkodex Adversarial Fixtures

These fixtures are inert templates for hostile validation. They intentionally avoid checked-in `.env` files and executable bad `.js` imports so Agentkodex's own quality gate is not poisoned. Tests generate live malicious files in temporary directories.

